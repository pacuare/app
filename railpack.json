{
  "$schema": "https://schema.railpack.com",
  "provider": "golang",
  "steps": {
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["go tool templ generate", "..."]
    }
  }
}
